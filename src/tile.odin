package main

import rl "vendor:raylib"

// Tiles are the nodes that the world uses to generate on morning.

Tile :: struct {
	pos:     rl.Vector2,
	texture: rl.Texture2D,
}

draw_tile :: proc(t: Tile) {

}

update_tile :: proc(t: Tile) {

}
